# MODE MODE — status (current as of 2026-08-16)

The live snapshot of where the site is. The arc is in `ROADMAP.md`; how work runs + how to onboard is in `ORCHESTRATION.md`. Provenance: **Verified** = read from the live file/git or headless-tested · **Decided** = Julia's call · **Open** = needs Julia/Matt.

## Last touched
- **2026-09-23** — **Studio invoice editing and removal (prepared for deployment).** Issued invoices can be edited with their number/payment ledger retained and previous PDFs available in Activity. Remove moves invoices out of active lists and balances into a restorable Removed view. Existing JSON storage supports the change without a schema migration. Browser verification uses isolated fixtures, including payment limits, failed/conflicting writes, PDF downloads and mobile layout. Workflow and deployment notes: `docs/STUDIO-INVOICES.md`.
- **2026-08-16** — **Editor: top + bottom height handles on text/box cards.** Julia: "why cant we drag the bottom and top of the column blocks, is that so crazy?" — right, only a bottom handle existed. Added a top handle (`.mm-rh-tt`) alongside the bottom (`.mm-rh-th`), both wired to a new `startBoxHeight(e,card,path,edge,handle)`: **bottom** edge → `min-height` (grows the box down, grow-only so copy is never clipped); **top** edge → `margin-top` (pushes the whole box down so its top *border* tracks the cursor while the content stays put). Each anchors the opposite edge and tracks the border it's on. The grid constraint is real and worth stating: a box can't extend *above* its row (nothing can move the row's top up), so the top handle moves the box down/lower and the bottom grows it down — you can't grow upward past the row top. `applyWidth` now applies both `min-height` and `margin-top` for non-image cards; image cards keep their own image-select crop resize untouched. `MM_BUILD` → `r15 · box top+bottom resize`. **Verified:** `node --check` clean; `?edit` DOM dump shows **both** handles on all 5 text cards and **0** on image cards; edit mode renders (r15 chip). **Drag feel unconfirmed under real pointer** — Julia to try both edges in the editor. Commit: `7527634`. Open question for her: if she wants a box to grow *upward* into the row above (not just move down), that's not expressible in this top-anchored grid without a bigger change.
- **2026-08-16** — **Grid, corrected: images keep matched baselines, text + stacks go ragged (fixes r13).** r13's global `align-items:start` over-corrected — it made *image* rows ragged too. Julia: "make sure the baselines of images match the previous commit not the current one." And text boxes **inside stacks** were getting enormous gaps (and resize "snapped back to the existing layout") because `.stackcell` used `grid-auto-rows:1fr`, which stretches a text child to the height of its tallest image sibling — independent of the container's align-items. **Fix, three parts:** (1) `.cards` back to **`align-items:stretch`** so image cards fill their row and bottom edges align (the previous-commit behavior she wants). (2) Text / non-image cards **and** stacks opt out with **`align-self:start`** — `.cards > .card:not(:has(img)):not(:has(video)), .cards > .stackcell{align-self:start}` — so they take natural height (short, ragged bottom, no white gap under the copy). Image cards (fill + fit-width) are *not* opted out, so they still stretch. (3) `.stackcell` `grid-auto-rows:1fr` → **`min-content`**: stack children take their natural heights, so a text child stays short and no longer re-stretches (which was both the huge gap and the resize snap-back). `MM_BUILD` → `r14 · images baseline, text ragged`. **Verified:** `node --check` clean; headless selected-arcade shows the HOVERBURGER text-in-stack hugging its full paragraph next to the tall one-sheet image (was: huge gap), classification text short, images stretching to fill. Uses `:has()` (Chrome/modern-browser baseline). Commit: `46af61c`. **The resize *feel* (no more snap-back) still needs Julia's confirmation under real pointer input** — the root cause (`1fr` re-stretch) is removed, but the drag itself is hers to try in the editor.
- **2026-08-16** — **Grid: ragged rows + height-resizable text boxes.** Julia on selected-arcade: "fix this column issue. it's ok if text boxes are short and one column doesnt always hit the baseline. but the text boxes need to be able to be resized height wise." **Two changes.** **(1) The white gap.** `.cards` was `align-items:stretch`, so every card in a row stretched to its tallest peer — a fit-to-width image or a text box sitting next to a taller image got white space below its content. Switched to **`align-items:start`**: cards take their natural height, ragged bottoms are fine (Julia's explicit call). This is *not* the reverted masonry — no `grid-auto-rows`/row-spans/dense flow, no gap-filling reflow; items stay in source order in their cells, tops aligned, next row clears the tallest. It reverses the old `stretch` preference that the masonry-revert had restored, but only that one property. **Also resolves the long-open "height handle fights stretch" item** — with stretch gone, an explicit per-card height just takes effect. Verified headless on selected-arcade, 65porter and eyeknow: white gaps gone, fill-images (`imgfit`) render at natural height with **no collapse**, nothing breaks sitewide. **(2) Text boxes height-resizable.** Before, *only images* got a resize handle (created in `selectImage` on select) and the renderer only applied a height to image cards — text boxes had no handle and no height at all. Added a **hover bottom-edge handle** (`.mm-rh-th`) to text / non-image cards **in the main grid** (`card.closest('.cards') && !card.querySelector('img,video,[data-img]')`), wired to the existing `startResize(…, 'y')`. It writes `b.height`, but for non-image boxes renders as **`min-height`** (grow-only) so a drag can never clip copy; images keep their hard-`height` crop. `applyWidth` now applies that `min-height` when `b.height>0` on a non-image card. Handle is edit-only, revealed on card hover; excluded from the bare portrait-lead copy column (not in `.cards`) so there's no dead handle where the render wouldn't honor it. `MM_BUILD` → `r13 · ragged rows + text height`. **Verified:** inline JS parses (`node --check`); an `?edit` DOM dump shows the handle on **all 5 text cards and 0 image cards**, and edit mode renders (r13 chip present = `enablePageBuilder` ran clean); public pages are byte-unchanged (edit-gated; `min-height` only applies when a height is authored). Commit: `a70b4c5`. **The drag *feel* is not yet confirmed under real pointer input** — per the drag-testing lesson, that needs Julia in the actual editor; the plumbing is verified but the interaction isn't.
- **2026-08-16** — **Lead-card type hierarchy: killed the ambiguous 15.5px middle tier.** Julia, on 65porter: the second line under the lede "reads more as a tag, small caption, or footnote… the narrative flow is a little weird." Root cause: the classification lead card had **three** type sizes — 20px lede (`.card.lead .body p:first-child`), **15.5px on every following paragraph** (`.card.lead .body p`), and 14px body everywhere else. That 15.5px middle tier is bigger than body, so it *emphasises* the follow-on paragraph; when that paragraph is a scope/credits line (65porter's "Office space design, 2019–2021. Conceptual, interior, architectural design…" and the RECOGNITION card's "Collaborators: …") it reads as a deck/tag, not copy, and the lede→follow jump feels off. **Checked all six projects' `classification.intro` before touching anything** (this is why she said "understand how it's used across the entire site"): the second paragraph is genuine secondary **narrative** in `massive`, `nightmare-kart` and `selected-arcade`; only `65porter` uses it as metadata (twice). So a special "scope/kicker" treatment would be *wrong* for the majority and is exactly the tag-like reading she flagged. **Fix — two roles, not three:** the lede (`:first-child`) is the single opening emphasis; everything under it is plain body. `.card.lead .body p` 15.5px/1.74 → **14px/1.78** (= `.body p`). Lede untouched. `eyeknow` (intro[0] is a `stack`) and `gif` (`layout:'two'` → `.body.two`, never gets `.lead`) are not `.card.lead`, so both are untouched. `MM_BUILD` → `r12 · lead type hierarchy`. **Verified:** inline JS still parses (`node --check`); headless before/after on 65porter (scope line now recedes to quiet body under the lede instead of an emphasised caption), `massive` and `selected-arcade` (clean lede → supporting paragraph, no regression). Commit: `c03b510`. **Not yet confirmed by Julia on the live site.**
- **2026-08-16** — **Removed the "Consequential Difference" document from the repo.** Julia: remove it from Mode Mode. It sat as **two orphan copies** in the repo root — `Consequential_Difference.html` and `consequential-difference.html` (same ~340KB editorial/interactive doc, 8 lines apart), both git-tracked but **referenced or linked from nowhere** in the site (the only mention was the STATUS aside below). `git rm`'d **both** — leaving the lowercase twin would have kept it live at `/consequential-difference.html`. They pulled in no local `assets/`/`content/` files, so nothing is orphaned; both remain recoverable from git history. Commit: `e6ed14d`.
- **2026-08-16** — **Mono unified on BB Strata ML — JetBrains Mono removed from the homepage + CMS.** Julia flagged the field-map homepage rendering its mono in **JetBrains Mono** (caught in the font inspector on `index.html`) — a default/AI tell, and out of step with the rest of the site. `project.html` and `about.html` already self-host **BB Strata ML** (`assets/fonts/BBStrata-MLRegular.woff2/.otf`, `@font-face` family `'BB Strata ML'`) as their mono/`--text` face; only `index.html` and `editor/index.html` were still on JetBrains, pulled from **Google Fonts**. **Scope was exactly those two files** — a repo-wide grep found JetBrains nowhere else. The two `Consequential_Difference.html` variants use a *system* mono stack (`ui-monospace,"SF Mono",…`), not JetBrains, and are a separate document, so they were left alone in that change (removed entirely in the entry above); `admin.html` is a redirect stub, also untouched. **Change:** dropped the Google Fonts `<link>` + `preconnect`s from both files, added the BB Strata ML `@font-face`, and pointed each `--mono` var at `'BB Strata ML',ui-monospace,Menlo,monospace`. **Two implications handled:** (1) the CMS lives in `editor/`, so its `@font-face` src is **`../assets/fonts/`**, not the root-relative path the other pages use — a wrong path here would have *silently* fallen back to system mono and still looked mono. (2) `index.html` draws label text to **canvas** ("CORE" + node labels), and canvas text does not re-lay-out when a web font finishes loading, so both `ctx.font` strings were re-quoted to `'"BB Strata ML",ui-monospace,monospace'` (matching project.html) and a `document.fonts.ready` repaint was added at the end of boot to force the **dirty-gated** field to redraw once the font resolves. The per-frame overlay labels already self-heal (they're redrawn every RAF frame, not gated), but the hook makes it correct-by-construction and mirrors project.html's existing `fonts.ready` handler. No Supabase/`fx.label` change needed — the canvas font family is hardcoded in JS, not stored in roster data. **Verified from several angles:** grep shows **0** JetBrains/googleapis/gstatic references left anywhere; `node --check` clean on both files' extracted inline JS; headless Chrome against the served real files confirms `document.fonts.check('20px "BB Strata ML"')` = **true** and FontFace status = **loaded** on *both* the root and `../assets` paths (isolated probe pages), and full-page renders of the homepage and CMS show the mono in BB Strata ML with the whole scene painting and no errors. Code: `a59d8e4`. **Not yet confirmed by Julia on the live site.**
- **2026-07-19** — **DATA LOSS FIXED: dragging a stack into its own child silently deleted blocks.** Julia's third recording (160s) — around 0:62 most of the canvas is simply gone, and she had to **undo** to get it back, so this was real model-level loss, not a render glitch. **Found by fuzzing, not by reading:** 23,862 random `stackDrop`/`reorder` sequences against the real functions extracted from source, asserting leaf-block conservation. Six failures, all the identical shape — source `nodes.N.content.blocks.M` (a stack), destination `nodes.N.content.blocks.M.blocks.K` (**one of its own children**). **Cause:** the mutators only guarded `src===dst`. Dropping a stack onto its own child splices the stack — children and all — out of the parent array and re-inserts it into its own `blocks`, making it self-referential and detaching every child from the document. Deltas were exactly −2 and −4: the orphaned child counts. **This was not an edge case** — a stack cell occupies the same screen area as its children, so dragging the cell and releasing over a child is the *normal* gesture, which is why she hit it within a minute. **Fix:** `isInside(dst,src)` (`dst===src || dst.startsWith(src+'.')`) now guards `reorder`, `stackDrop` **and** all three drag handlers, so the UI won't even show a drop indicator for an invalid target. **Verified:** re-fuzzed at **119,300 ops across all six documents** mixing `stackDrop`/`reorder`/`delBlock` — 0 block loss, 0 over-nesting, 0 throws (was: reproducible loss). Targeted suite 7/7, including "stack into its own child is refused" and "stack onto itself is refused" while legitimate wrap/append/insert-after all still work. `MM_BUILD` → `r11 · self-drop guard`. **Still open from the same recording, NOT yet fixed** — (1) **scaling:** the bottom height handle writes a hard `height`, which overrides `align-items:stretch`, so a resized card stops matching its row and there is no way to clear it. Per-card height is fundamentally at odds with an aligned-row grid; the honest fix is probably for that handle to drive the image **crop** rather than the card height. Needs Julia's call. (2) **"can't get to one column":** inside a stack, `.stackcell > .card` forces `grid-column:1/-1`, so cycling ▭ on a *child* is a visual no-op — the button appears to do nothing. Either hide ▭ on stack children or have it drive the parent stack's width.
- **2026-07-19** — **Stacks: a row cell can hold two blocks, rows stay aligned.** The thing Julia has been asking for since "i cant drag something up to that row and have it sit UNDERNEATH the 1/3" — done properly this time, after masonry was the wrong answer. **Shape confirmed with a low-fi mockup in chat before any code**, which is what stopped a second wrong build. **Model:** a new block type `{type:'stack', width, blocks:[…]}` occupying **one cell** of the 12-column row. Capped at **one level** — `normalizeStacks()` flattens any nested stack, collapses a 1-child stack back to the child (inheriting the stack's width) and drops an empty one, so the model can't drift into a shape the renderer doesn't handle. It runs after every `reorder`, `delBlock` and `stackDrop`. **Render:** `blockCards` emits a `.stackcell` — a nested 1-col grid with `grid-auto-rows:1fr`, so children divide the cell's height. The cell takes the width and `data-block` of the stack (applied by the caller); children set their own nested `data-block`. Two things that would have broken it: the width rules were scoped to `.card`, so `.stackcell` needed its own `w-*` spans, and children carry `.card`'s `grid-column:span 6`, which inside a 1-col grid would have created **implicit columns** — both neutralised. Cell is `position:relative` and its toolbar sits left so it can't collide with its first child's. **Gesture:** drop on a card's **bottom edge** → stack. Left/right still mean "new cell in the row", top/bottom still mean before/after everywhere else; full-width targets are excluded (stacking in a single-cell row is meaningless). Dropping on a stack cell appends; dropping on a block already in a stack inserts after it. **Editor Form tab** got a `stack` branch in `blockBody` — it previously fell through the type chain and rendered an empty row. **Verified:** 12/12 model tests against the real `stackOwner`/`normalizeStacks`/`stackDrop` pulled from source (wrap, append, insert-after, collapse, flatten, empty-removal, self-drop no-op, no blocks lost); live drag on a local server built a stack whose cell measured **721px against its 721px row peer with bottom edges aligned**, children at 349+349+22 gap; all six projects re-checked — 0 overlaps, no errors, card counts unchanged. Both files syntax-clean. `MM_BUILD` → `r10 · stacks`. **Not yet confirmed by Julia.**
- **2026-07-19** — **Masonry REVERTED — wrong call, mine.** Julia on seeing `193ba58` live: "hmm, no it's busted… you were closer before this round." She is right, and the misread was mine, not hers. She picked "column masonry" from options **I** wrote, but what she has been describing all along is not free-form masonry — it is a **tidy row grid that can hold a stacked column**. Free masonry removed the row alignment entirely, and the aligned editorial grid is the thing the whole site's look rests on: the result was ragged columns and floating voids. `.cards` is back to `align-items:stretch`, `layoutMasonry`/`watchMedia`/`relayoutSoon` and the `grid-auto-rows`/`dense` CSS are gone, and a small `clearSpans()` runs on render so no stale inline `grid-row` from `r8` can survive in a cached canvas. **Kept from the reverted commit:** the height handle still writes `height` rather than `min-height` — an explicit height overrides `align-items:stretch`, so the bottom handle can genuinely crop even with tidy rows back. Also kept: the `dragenter` fix, `fitRow` row-rebalancing, width cycling, and the build chip. `MM_BUILD` → `r9 · masonry reverted`. **Lesson for whoever picks up stacking next:** do not reach for masonry again. The target is rows that stay aligned, where a single row cell can contain **two stacked blocks** beside a taller one — i.e. the *nested stack container* option (blocks nest one level), which Julia did not choose only because I framed masonry as the recommended one. Confirm the shape with her before building it.
- **2026-07-19** — **Masonry (real stacking) + the bottom height handle now crops.** Julia: "still no stackability." **Both problems were the same line:** `.cards` had `align-items:stretch`, which locks every card to the height of the tallest in its row. That is what made a short block reserve a whole row (nothing could sit *underneath* a third), and it is also why the bottom resize handle did nothing — height was written as `min-height`, which can only push a card *taller*; stretch pinned the floor at the row height so shrinking was impossible. **Masonry:** `.cards` becomes `align-items:start` on `grid-auto-rows:8px` with `grid-auto-flow:row dense`, and `layoutMasonry()` gives each card a `grid-row: span N` sized from its own content. Widths are untouched — the same 12-column spans. Cards have `overflow:hidden`, so `scrollHeight` reports full content height even while the card is span-constrained, which is what makes the sizing converge instead of oscillating. Re-runs on image `load`/`error`, on resize, and at 0/300/1200ms. An explicit inline height wins over measured content, so a cropped card spans fewer rows. **Height handle:** writes `height` (not `min-height`) and relayouts live; `imageCard` applies stored height the same way. **A near-miss worth recording:** the first working version put the layout call only in `renderDoc()` — the *editor's* entry point. The published page calls `render()` directly, so it got the new CSS with **no spans**, every card collapsed to a single 8px row, and local testing showed **14 overlapping cards**. That would have shipped a wrecked portfolio. The call now lives at the end of `render()`, the single choke point every path goes through. **Verified locally against a `python3 -m http.server` copy of the repo, before any push** — all six projects, images forced eager and given time to settle: **0 overlaps, 0 missing spans, 0 cards clipping content**, on Eyeknow with 22/23 images loaded and card heights ranging 104px–1225px. One transient overlap on `selected-arcade` was a measurement artifact of reading rects mid-load; it disappears with a longer settle, and CSS grid cannot truly overlap auto-placed items. Screenshots of GIF and Eyeknow checked by eye. `MM_BUILD` → `r8 · masonry+height`. **Consequence Julia accepted:** this changes the **published** pages, not just the editor — the equal-height row look is gone by design, and the `429b0d4` crop/fill behaviour that keyed off baseline-locked rows no longer has stretched rows to fill. **Not yet confirmed by Julia, and not yet checked on the deployed site with production media.**
- **2026-07-19** — **Row-fit never fired for most drops — my own bug, same commit as the feature.** Julia's second recording (GIF project, 48s): reorder works, the layout visibly rearranges all through the clip, but IMG-02/IMG-03 stay two halves in a row with IMG-04 stranded below — it never goes three-across. **Cause:** `a23f3b9` gated `fitRow()` on `beside` — only a drop whose nearest edge was **left or right**. The reasoning was that top/bottom means "stack". But these cards are **wide and short**: for IMG-03 in the recording (305×182 px), the left/right wedges are only the two triangles at the ends, so **70.1% of the card's area registers as top/bottom** (measured by Monte Carlo over the real rect, 400k samples). Most of Julia's drops therefore ran the reorder and skipped the rebalance — the block moved in document order, stayed a half, and wrapped below. That is precisely "it moves but it won't go next to them", and it is why the layout kept changing while never reaching three-up. **Fix:** the gate is gone. Dropping onto a card means "put it in that card's row"; the nearest edge now only decides **before vs after** ordering, which is what the indicator has always shown. True stacking-under is a masonry problem, not something the edge test can express in a 12-column grid — deferred to that work, as agreed. **Also added `MM_BUILD`** — a small build chip pinned bottom-left of the canvas in edit mode (`r7 · rowfit-all-edges`). Four rounds of this thread were spent unable to tell whether Julia was running the code I had just shipped; the chip removes that question permanently. **Bump `MM_BUILD` on every project.html change that ships.** project.html syntax-clean. **Live re-verification blocked:** Chrome control froze behind a native "Leave site?" dialog raised by switching projects with unsaved changes, so this round is headless + geometric only — **not confirmed live, and not confirmed by Julia.**
- **2026-07-19** — **Width cycling + row-fit on drop.** Julia: "I still can't drag a block up to sit next to a different row." **Likely not a drag bug any more** — `.cards` is a 12-column grid (`full`=12, `two-thirds`=8, `half`=6, `third`=4) with implicit auto-flow, so a row of two halves is *already full at 12*. The drop now succeeds and the block moves correctly in the document, but the grid has nowhere to put it beside them and wraps it to the next row. It reads as "it won't go next to that row." **Fix (Julia's rule — equalise only when it would overflow):** after a drop, `fitRow()` compares the moved card's rendered `top` against the target's. Same row ⇒ it fit, nothing touched, everyone keeps their hand-set widths. Different row ⇒ it overflowed, so the target's row members plus the moved block are equalised to `12/N` (2 ⇒ half, 3 ⇒ third). Rows are read **geometrically from the rendered DOM** rather than by re-deriving grid auto-flow, so they match what is actually on screen. **Only fires on a left/right (beside) drop** — a top/bottom drop means "before/after in the flow", and rebalancing there would override widths when the intent was to stack. `reorder()` now returns the moved block's new path so the row can be located after the re-render. **Known limit:** caps at 3 across, because `third` (span 4) is the narrowest width — 4-up needs a new quarter (span 3) class; `fitRow` returns false and leaves the wrap rather than doing something arbitrary. **Also: ▭ cycles instead of opening a menu** (Julia: "the drop down kind of breaks flow") — click steps full → two-thirds → half → third → full, ⇧-click reverses. Applies to **every** card, so text blocks get the same control as images, which was the other ask. Verified headless against the real `DEFW`/`WORDER`/`WEQ`/`widthOf` pulled from source: cycle wraps correctly in both directions, all 8 block-type defaults resolve, legacy `wide:true` ⇒ full, and an explicit `width` beats `wide`. project.html syntax-clean. **NOT yet confirmed by Julia — do not treat as closed.** **Open, agreed with Julia:** (1) stacking a block *underneath* a ⅓ — she chose **column masonry for the canvas**; flagged to her that this changes the **published** pages, not just the editor, and undoes the `align-items:stretch` baseline-locked row heights that the `429b0d4` crop/fill behaviour is built on. (2) **Bottom height-resize doesn't work** — reported, not yet reproduced. (3) Empty sections as drop targets, auto-filling width.
- **2026-07-19** — **Background colour, round 3: black text on saturated grounds, nav padding.** (1) **The ink pole was picked by max contrast, which is the wrong heuristic** — it flips at points a designer wouldn't expect. On `#00AFED` it scored dark ink 7.4:1 against light ink 2.5:1 and duly produced black body text on brand blue. Now picked by **perceived lightness** (`relLum(ground) < 0.4`, the standard light/dark test). (2) **Two thresholds disagreed**, which is why the first pass fixed the body text but left a dark navy title: `derivePalette()` used 0.4 for the ink pole while `legibleInk()` still used 0.18 for the accent, so on a mid-tone ground the page got light copy and a dark H1. Both are 0.4 now — one test for the whole palette. (3) **Section nav had 2px of horizontal padding**; now 14px, with the hover shift riding on top (7px → 21px) so the animation still reads. (4) The picker reports **both** numbers now — text contrast and accent contrast — because when the accent and ground are near-identical the accent ratio is the one that explains the problem. Calibration at `#FFFFFF` still exact; all six accents on white unchanged; 6-page headless run still pixel-identical to the original baseline. **Honest limit:** `#00AFED` is a hard ground for this project — gif's core `#00A7FF` is essentially the same colour, so text lands at 2.17:1 and the accent at 2.41:1. Nothing in the solver can fix same-on-same; the readout goes red so the ground can be re-picked.
- **2026-07-19** — **Background colour, round 2: blob dissolve, nav spacing, section headers, toolbar collision.** Julia's feedback on the shipped control. (1) **The blob now blends.** I was wrong last round — I checked navy and green, concluded the alpha falloff was enough, and didn't check true black, where the pale authored edge reads as an obvious white haze. New `adaptEdge()` re-seats the edge on the active ground: hold hue + saturation, mirror the lightness as the ground darkens (`l' = mix(1-l, l, Lg)`). Eyeknow's `#DDDAFC` becomes `#060325` on black, so the falloff terminates *in* the ground and the pink core still reads. **Identity is exact at `#FFFFFF` for all six project edges** — verified, no round-trip drift. (2) **Section nav** was cramped: `flex:1 1 0` with `min-height:0` let rows collapse to ~21px. Now `min-height:31px`, `gap:14px`, `padding:4px 2px`, with the nav scrolling if a project has too many nodes to fit at that size. (3) **Section headers** (`.card-h .lbl`) take a new `--label` token — `var(--accent)` on light grounds, `var(--grey)` on dark, matching the stat labels (`HOSTS`, `LOCATION`) as asked. Set in `applyPalette()` off the ground's lightness. (4) **The block toolbar no longer collides with the card tag** — the tag fades on card hover. Worth knowing: **that tag is auto-derived and not editable** (it's the block kind), so nothing is being hidden from you; it's the `DETAIL`/`IMG-NN` label, distinct from the stat labels. (5) **Swept the remaining hardcoded light-ground assumptions** that were breaking on dark: the minimap + field label chips were painted `rgba(255,255,255,.9)` (the white boxes on black), the unhovered node dot was `#FFFFFF`, label text `#15140F`/`#AEACA4`, `.card:hover` border `rgba(18,16,12,.34)`, `.car-btn`, `.mm-tools` and `.mm-menu` backgrounds. All now read the derived palette via a module-level `PAL`. Verified: syntax clean, zero page errors, 6-page headless run **still pixel-identical to the original baseline** on light grounds. **Open:** the hover label's *placement* — it can land on top of the H1; Julia asked for it "above the title" and I didn't want to guess which reading she meant, so it's unresolved.
- **2026-07-19** — **THE snap-back cause: `dragenter` was never `preventDefault`ed.** Julia said the reorder still didn't work after `560435a`/`b79d161`, and she was right. Reproduced under **real CDP mouse input** (real hover → real mousedown on ⠿ → real drag), with every drag event logged at document capture **and** bubble. The failing tail is unambiguous: `dragenter` on the target at (848,206) → `dragleave` **at the same coordinates** → `dragend`, with **no `dragover` in between**, and the `dragover:after` prevented-flags reading `[true,false,false]`. **Cause:** the HTML5 drag protocol only accepts a drop where a **prevented `dragover`** has occurred, and the card bound `dragover` but never `dragenter`. `dragover` only re-fires on movement or roughly every 350ms, so when the pointer arrives at the target and the button releases before the next `dragover`, the element was never registered as a valid drop target and the browser cancels the drag → the block springs back to where it started. **This is why it was intermittent, and why it survived three rounds of fixes:** linger over the target and the drop lands, flick and release and it dies. It is orthogonal to the `sameArray` gate (also real, also fixed in `560435a`) — that gate blocked cross-section drops outright; this one drops *any* brisk drag, same-array or not. **Fix:** `card.addEventListener('dragenter', …)` with the same guard as `dragover`, calling `preventDefault()` and setting `dropEffect='move'`, so the target is valid the instant the pointer enters it. **Verified as a controlled A/B on the live page** — same card, same start and end coordinates, same drag speed: *before* the patch, `drop` never fires, `dragend` fires, nothing moves; *after*, `drop` fires, the block re-parents from `nodes.0.content.blocks` into `classification.intro` and lands at index 5, immediately after the drop target at index 4 — exactly where the bottom-edge indicator promised. 30 cards before and after, dirty flag set, test move reverted so the doc is untouched. **This closes the cross-array-under-real-input gap flagged in the entry below.** **Method lesson, recorded because it cost three rounds:** synthetic `DragEvent`s cannot trigger the CSS `:hover` that reveals the toolbar, cannot exercise the `draggable` attribute, and — critically — fire `dragover` on demand, so they *mask* exactly this class of bug. Drag work must be verified with real pointer input; synthetic dispatch only proves `reorder()`'s arithmetic.
- **2026-07-19** — **Reorder verified under a *real* drag; `dragend` cleanup fixed.** Julia reported `560435a` still didn't work. **Method failure on my side, twice:** verification used synthetic `DragEvent`s dispatched straight at the card, which cannot trigger the CSS `:hover` that reveals the toolbar (`.mm-pbcard:hover > .mm-tools{display:flex}`; it is `display:none` otherwise). Synthetic dispatch therefore bypassed the ⠿ handle, the `draggable` attribute and the browser's native drag machinery entirely — it only ever proved `reorder()`'s arithmetic, never the path a hand actually takes. **Re-tested with CDP-driven mouse input** (real hover → real mousedown on ⠿ → real drag), with every drag event logged at document capture **and** bubble inside the canvas: `dragstart` fires, `dragover` on the target reports `defaultPrevented:true` after the card handler, `drop` reaches the card handler, `reorder()` runs, and the two blocks visibly swap with the dirty flag set. The shipped code is correct under a genuine drag. **Real defect found by that log:** `dragend` **never fires** after a successful drop — `reorder()` calls `renderDoc()`, which rebuilds the DOM and destroys the source element mid-drag, taking its `dragend` listener with it. `_dragSrc` was left stale and `.mm-dragging` / `draggable` could strand on recycled nodes. The `drop` handler now clears `_dragSrc`, the four indicator classes, `.mm-dragging` and any leftover `draggable` itself rather than relying on `dragend`. **Caveat on the real-drag test:** the pair it moved was *same-array* (`nodes.0.1`→`nodes.0.0`) — a case that was never broken. Cross-array is proven by the headless sweep and by synthetic dispatch (which did re-parent `nodes.0`→`nodes.2`), and there is no array-dependent branch left anywhere in the event path — the guards are now identical for both — but a cross-array move has **not** been driven by real mouse input. **Suspected cause of Julia's continued failure:** the canvas iframe is cache-busted (`openCanvas()` appends `&t=Date.now()`) but *only when a project is selected*, so a canvas opened before the deploy keeps running the old `project.html` until the editor page is reloaded or the project re-picked. Visible tell: on the new build, dragging over **any** card shows an accent drop-bar; on the old build a cross-section target shows nothing. **Status: UNCONFIRMED pending Julia — do not mark the original report closed again without her word.** **Open:** empty sections still cannot receive drops (no card to aim at); Julia has asked for that, plus auto-fill width on drop-in.
- **2026-07-19** — **Concurrency note + cross-check.** Two commits landed on this working copy from another session while I was working — `560435a` (masonry reorder: drops across sections) and `af01010` (Supabase-on-Pro docs, which already covers what I was about to write). My pushes fast-forwarded cleanly and my working tree was empty at each commit, so nothing of anyone's was swept up, but **the repo is being edited concurrently and this is worth knowing before trusting any single session's picture of the file.** `560435a` rewrote `reorder()` and both card drag handlers — the same code I fixed earlier today — so I re-checked: the tie-break chain (`mn===dl?false:…`), the `_bpath` crop/resize fix and the non-file drag guard are all still intact, `sameArray` is gone with **no dangling references**, `arrPath` is defined, and `project.html` is syntax-clean. Combined 6-page headless run is **pixel-identical to the original baseline**. Also fixed the one line the Pro update missed — the architecture section still claimed a "Supabase default ~50 MB/file".
- **2026-07-19** — **Per-project background colour, part 2: the editor control.** A circular swatch sits bottom-right in the editor chrome, directly above the savebar (30px in, 62px up — verified by measurement, swatch bottom 698px vs bar top 701px). Clicking opens a popover with a native colour picker, a hex field, a live contrast readout and a reset-to-white. Writes `colors.ground` onto the doc through the existing `markDirty()` path, so it saves with everything else and the debounced `mm-edit-load` sync repaints the canvas live. Visibility mirrors the savebar. **The contrast readout doesn't duplicate the maths** — `project.html` posts a `mm-palette` message with the derived ink and its achieved ratio after every `applyPalette()`, and the editor just displays it, so the two can't drift. Reads red below 4.5:1, which is the honest signal when the chosen ground is itself the constraint (a mid-tone grey caps out around 4.3:1 no matter what). Verified: editor module parses clean, zero page errors, placement measured and screenshotted; end-to-end colour change already verified through `project.html` on `#0A0A0C`, `#0E1A2E` and `#1B4D3E`. **Not yet click-tested live** — Julia to confirm the feel once deployed.
- **2026-07-19** — **Per-project background colour, part 1: the derivation chain.** `project.html` now reads `colors.ground` off the project doc (absent ⇒ `#FFFFFF`, so all six existing docs work untouched — no row migration) and derives the whole palette from that single value: `--panel`, `--ink`, `--grey`, `--grey-2`, `--hair`, `--hair-2`, `--scrim`, and `--accent` via the ground-aware `legibleInk()`. **Greys take the ground's hue** (Julia's call) — they ride the ink→ground axis with *per-channel* positions measured off the original ramp, which is what preserves its warmth; on a navy ground you get blue-greys, on green, green-greys. **The constants are calibrated so `#FFFFFF` reproduces the original hand-picked palette byte-for-byte** — `--ink:#15140F`, `--grey:#7E7D75`, `--grey-2:#B6B5AD`, hair base `#12100C` — asserted in test, and the 6-page headless run is still pixel-identical to the pre-change baseline. Ink picks its pole by *achieved contrast* rather than by ground lightness: on a mid-tone ground (`#7A7A7A`) dark ink beats light ink 4.3:1 vs 3.7:1, and choosing by lightness alone would have taken the worse one. **The blob needs no fix.** The proposal assumed the metaball edge is authored to dissolve into white and would film on colour; it isn't — the renderer fades by *alpha* (`d[q+3]=a*255`), so it composites correctly over any ground. Rendered on `#0A0A0C`, `#0E1A2E` and `#1B4D3E` and looked: it reads as a soft glow, no film. That removes the riskiest planned commit — nothing needs to touch `index.html`'s READ-ONLY minimap mirror. **Known tradeoff:** grounds close to the accent's hue wash it out — on deep green `#1B4D3E` the eyeknow pink solves to `rgb(253,128,179)` to clear 4:1. Worth surfacing as a live contrast readout in the picker.
- **2026-07-19** — **Supabase is on Pro ($25/mo) — the free-tier idle-pause is no longer a constraint.** Verified against the Supabase Management API, not assumed: org `jalulia's` (`yufqhywdbqzljrgczxyn`) returns `"plan":"pro"`. Docs updated to match — `STATUS.md` (billing section + the storage-cap note), `ROADMAP.md` (Phase 11 + immediate next moves), `docs/supabase/README.md` (status banner + the cost rationale). **Consequence worth acting on:** `.github/workflows/keepalive.yml` existed *only* to ping the project every ~2 days so a free project wouldn't sleep after 7 days idle. That job is now pointless — the workflow header says so, and the file can be deleted at will (left in place for now; harmless either way, it's one cheap request). Pro's other effects are ceilings the site is nowhere near (8 GB DB / 100 GB storage + egress) plus PITR availability, so nothing else in the architecture changes. No code touched.
- **2026-07-19** — **Masonry reorder: drops across sections now land** (root cause reproduced live, not guessed). Julia's screen recording showed blocks still springing back after `edcc35d`/`f147a13`. Those two commits were correctly applied and were **not** the problem — they fixed the image file-drop interference; a guard *upstream* of them was rejecting most drags before they ever reached that code. **Cause:** the Canvas masonry looks like one flat grid, but its cards are emitted from **11 separate arrays** — `classification.intro` plus one `nodes.N.content.blocks` per content node (`renderDoc` lines ~664–670) — and `dragover`, `drop` and `reorder()` all bailed on `!sameArray(src,dst)`, which only permits moves between siblings of the *same* array. Measured live on Eyeknow Manor (30 cards / 11 arrays): **778 of the 870 possible drags — 89.4% — were silently rejected**, no drop bar, no cursor change, just snap-back; 3 cards are the sole occupant of their array and so could not be moved anywhere at all. That is why it read as intermittent — drags inside `nodes.2` (7 blocks) landed, anything crossing a section boundary died. **Decision (Julia):** free reorder across sections. Visual order *is* section order, so a card cannot move past a section boundary without changing which section owns it — re-parenting is the only way the masonry can behave the way it looks. Empty sections are already a normal state (nodes 3/6/8/9/13 ship with zero blocks), so this introduces no new condition. **Fix:** `sameArray()` → `arrPath()`; `reorder()` now resolves source and destination arrays independently, splices out of one and into the other, and applies the `si<di` index correction **only when both are the same array**; the `dragover`/`drop` guards drop the same-array test, `dragover` sets `dropEffect='move'`, and `drop` `stopPropagation`s so a drop can't be double-handled. **Verified headless against the real `getByPath`/`arrPath`/`reorder` extracted from `project.html`** (not reimplementations, so the tests can't drift from source), across all 6 project docs: **1,516 moves — 212 same-array + 1,304 cross-array — 0 throws, 0 blocks lost, block count invariant**, and **1,516/1,516 landed in the exact position the drop indicator promised** (immediately before the target on a left/top edge, immediately after on a right/bottom edge). project.html syntax-clean. **Limitation:** a section with zero blocks has no card to drop onto and so cannot receive one — use `＋ Add block after` there. Real pointer-drag feel still wants a by-hand pass; synthetic events exercise the same handlers but not the native drag.
- **2026-07-19** — **Reverted the grey.** Steps 1–2 below unified `project.html` onto `index.html`'s palette *values*, which moved the ground `#FFFFFF`→`#F3F3F0` and every panel to `#FBFBF9`. That was a look decision Julia never asked for — she wants an **editor-controlled background colour per project**, not a light/dark theme. Palette values restored to the originals (`--ground`/`--panel` `#FFFFFF`, `--ink:#15140F`, `--grey:#7E7D75`, `--grey-2:#B6B5AD`, `--hair` .17/.08, `--scrim` white); verified headless as **pixel-identical to the pre-change baseline across all 6 project pages** (accent, every surface, card counts). What's kept is the *structure*, which is exactly the machinery the real feature needs: split `--ground`/`--panel` tokens, the `--scrim` token, and a `legibleInk()` that solves against the active ground in either direction. Steps 1–2 are therefore re-framed as groundwork for the colour control, not as dark mode.
- **2026-07-19** — **Dark mode, step 2 of 6: `legibleInk()` learns to lighten.** Step 1 made the solver read the active ground, but it could still only *darken* (scale RGB by `f<1`) — on a dark ground that walks the wrong way and bottoms out near black. It now branches on ground luminance (`relLum(ground)<0.18`): light grounds darken toward the ink exactly as before, dark grounds hold hue + saturation and raise **lightness** via an HSL round-trip (`rgb2hsl`/`hsl2rgb`). Scaling RGB upward was not an option — it clips and skews hue; HSL moves only the value, which is the same rule the field edges follow. **Pure addition, verified as such:** on `#F3F3F0` all six cores return byte-identical results to what step 1 shipped, and a headless run of all 6 project pages is identical to step 1 on accent, every surface, and card counts. On `#0A0A0C` all six clear the 4.0 floor with ≤0.06° hue drift — eyeknow `#D70359`→`#E1035D`, nightmare `#C40021`→`#E30026` (the two that were under), the other four already above and passed through untouched. Tests pull the *real* functions out of `project.html` rather than reimplementing them, so they can't drift from the source. **Note for step 3:** the default target is 4.0, but the small accent uses (`.ident .code` 11.5px, `.nav-item .lab` 13px, `.card-h .lbl` 11px) are normal text and want AA 4.5 — at 4.5 the dark accents become eyeknow `#F50365`, massive `#2F7AE3`, nightmare `#F7002A`; gif/65porter/arcade are unaffected. Pass `4.5` at the dark call site rather than changing the default, which light mode depends on.
- **2026-07-19** — **Dark mode, step 1 of 6: `project.html` adopts the canonical token set.** The two surfaces had drifted — `index.html` was `--ground:#F3F3F0` / `--panel:#FBFBF9` / `--ink:#16160F`, `project.html` was a single `--paper:#FFFFFF` / `--ink:#15140F` with no panel token — so theming either one would have pulled them apart. `project.html` now uses index's names **and** values (index is the spine): `--paper` splits into `--ground` (html + body) and `--panel` (all 9 surfaces — `.card`, `.nav`, `.hero-head`, `.hero-portrait`, `.ct-img`, `.cropfig`, `.cropfig-rect`, `.imgfill`, `.mm-inplace`), and `--ink`/`--grey`/`--grey-2`/`--hair`/`--hair-2` take index's values. **`legibleInk()` now solves against the active `--ground`** (read from CSS via `groundRGB()`) instead of hardcoded white — necessary, because moving the ground off `#FFFFFF` silently dropped three projects under the function's own 4.0:1 contract (gif 3.76, 65porter 3.63, arcade 3.72). Accents shift accordingly: gif `#0082C7`→`#007CBD`, 65porter `#5A8C15`→`#548314`, arcade `#818106`→`#787805`; eyeknow/massive/nightmare unchanged. **Caught in verification:** the three blob ground-fades (`.field::after`, `.field-scrim`, the nav fade) were hardcoded to `#fff`, invisible on a white ground but a visible pale patch behind the wordmark on `#F3F3F0` — they now read a new `--scrim` token, which is also the mechanism dark mode needs, so the planned scrim commit collapses into this one. Verified headless (Playwright, all 6 project pages): ground `rgb(243,243,240)` and every panel `rgb(251,251,249)` on all six, all six accents ≥4.0:1 on the ground, card counts unchanged, **no new console errors** (6 before, 6 after — all pre-existing 401s on hotlinked media plus a third-party player artefact; an apparent +2 was a flaky baseline, confirmed by re-running HEAD in the same session). Before/after screenshots compared.
- **2026-07-19** — **Canvas crop + resize now actually save** (root cause reproduced live, not guessed). Symptom: cropping or resizing an image on the Canvas changed nothing — no "unsaved changes" flag, nothing persisted. **Cause:** `[data-img]` carries the **src** path (`nodes.0.content.blocks.1.src`), but `selectImage()` handed it straight to `getByPath()` as if it were a **block** path, so `block` came back as the URL **string**. Under the file's `"use strict"` (line 239), `cropBoxUI`'s `save()` doing `obj.crop={…}` threw `TypeError: Cannot create property 'crop' on string …` and died *before* `commit()`→`postDoc()` — so no `mm-edit-doc` ever reached the editor. `startResize()`'s `b.width=curW` threw identically, before its own `postDoc()`. Both failures were **silent**: the throw happens inside an event handler, and `postDoc`'s `try{}catch(e){}` is empty. Not the drawer-sync echo (`_applyingCanvasEdit`) — that guard is correct. **Fix:** `selectImage()` / `openCropPanel()` split the src path into block-path + key (`…blocks.1` + `src`), resolve the *block*, pass the real key to `cropBoxUI`, and guard `typeof block==='object'`; the resize handles now get the block path too, and `startResize` type-guards its write. **Reproduced live** in the deployed editor over Chrome — a probe counting `mm-edit-doc` into the parent showed a control inline-text edit → 1 message + dirty flag, vs. crop → **0 messages**, with the exact TypeError in the console. Verified across all 6 project docs: **38/38** image src paths resolved to a non-object under the old code (i.e. every crop/resize on every image was broken), **38/38** now resolve to a real block with the key round-tripping; project.html syntax-clean.
- **2026-07-19** — Block reorder: snap-back fixed + stack-underneath (`edcc35d`, plus tie-break fix this session). **Snap-back root cause:** every `[data-img]` carried file-drop handlers that swallowed card-reorder drags — image `dragover` always `preventDefault`ed, and image `drop` called `stopPropagation()` *before* checking whether a file was present, so the card's reorder `drop` never fired and the block sprang back. Both handlers now bail early on a non-file drag (`dragover` requires `Files` in `dataTransfer.types`; `drop` returns before `stopPropagation` when there's no file), letting the reorder drop through. **Stack-underneath:** the card drop previously read only `clientX`, so a block could only land left/right of a target — never below. Drop detection now measures all four edges (`dl/dr/dt/db`, nearest wins) with matching `mm-drop-l/r/t/b` shadow indicators, so dropping near the bottom edge stacks a block *under* the target. **Tie-break bug found in verification + fixed:** the drop handler derived `after` as `(mn===dr||mn===db)` — an independent comparison chain from the one picking the indicator class — so on an exact distance tie (dead-centre, or a corner where `dl===db`) the hairline showed *before* on the left edge while the block actually inserted *after*. `after` is now derived from the same l→r→t→b priority chain as the class. Verified: 8/8 edge cases, and **0 indicator/action disagreements across all 40,401 points** in a test card (was 2 tie classes); project.html syntax-clean.
- **2026-07-19** — Image slots accept video / MP4 (`06c71c4`). Bugs: the drop handler filtered to `image/` (an MP4 highlighted on drag-over but the drop did nothing) and both file pickers had `accept="image/*"`; and a video src rendered as `<img>` (can't play). Fixed: drop + both pickers now accept `image/*,video/*`; dropped/uploaded files transfer as the **File** itself (no base64) and upload directly to `project-media`; a **video src renders as an autoplay + loop + muted + playsinline `<video>`** that cover-fills the frame (Matt's original note). Bucket already allows all MIME + no size cap (checked live). Verified headless (isVideoSrc 7/7; video render attrs + cover). NOTE: single uploads are capped at 50MB by the bucket's default limit, so huge MP4s (300MB+) won't fit as-is — use short web-sized loops. (On Pro that cap is raisable in Storage settings, but a 300MB MP4 on a portfolio page is the wrong fix.) Video isn't croppable yet (always cover-fills) — follow-up if wanted.
- **2026-07-19** — CMS: double-click drawer + height resize + slim single-click (`70b2460`). Per Julia's morning notes. (1) **Double-click an image → left field drawer** (slides from the nav) with that block's Form fields — replace/upload, crop, alt, caption, fit, wide — reusing `blockBody`; drawer edits sync live to the canvas (debounced `mm-edit-load`, guarded against canvas-origin loops). (2) **Single-click = quick crop only** — removed the cluttered Crop/Layout/Delete toolbar + aspect-dropdown from the select state; now it's the crop box + resize handles. (3) **Height resize** — resize handles gained a bottom + corner handle; dragging sets a fluid `block.height`, rendered as card `min-height` so the row **re-baselines** (siblings stretch to match). Height re-baseline + grid-snap verified headless; both files syntax-clean. **Not yet click-tested live** (that sandbox is still down) — Julia to confirm the drawer/double-click/height feel. This supersedes the earlier "select-state clutter" known issue.
- **2026-07-19** — Empty-image slot made functional + drag-drop upload (`20e9171`). Clicking an empty `+` slot opens the Add-image panel; dropping an image file onto any slot uploads it (canvas → dataURL → editor → Supabase). Both files syntax-clean.
- **2026-07-19** — Fix cramped two-column (`43e962d`). "THE MANOR" text block had `layout:'two'` on a **third-width** card, forcing two 3-word columns. Scoped `.body.two` to `.card.w-full`/`.card.wide` only — narrow cards render one column regardless of the flag. Verified headless.
- **2026-07-19** — Direct-manipulation resize (Figma frame+fill model) (`e5f04e0`). Selecting an image outlines its card and shows **drag handles** (right edge + SE corner); dragging resizes the element and **snaps its width to the grid** live. Scale = handles on the element; crop = the crop box on the content. Grid-snap math headless-verified 3/3. Layout dropdown keeps size/fit as a fallback. **KNOWN ISSUE:** the select-state UI is cluttered/overlapping (card toolbar + image toolbar + crop panel + aspect dropdown all stacking top-right) — needs consolidation into one clean contextual toolbar; blocked on driving the live editor (sandbox transport down) to fix against the real DOM.
- **2026-07-19** — Scale + crop as two controls (`81f7917`). Image Layout ▾ got a Size section (Full/Two-thirds/Half/Third) + Fit + Replace; crop preserved across resizes. (Superseded as the primary mechanism by the drag handles above; dropdown kept as fallback.)
- **2026-07-19** — Canvas image UX redesign + one-column text (`3208d0b`). (1) Full-width text is **one column** again. (2) Clicking an image **selects** it → Crop / Layout ▾ / Delete toolbar in the upper-right; crop opens **directly over the image in place**, not a detached panel. Removed the redundant card crop button and the swap-prompt-on-click. project.html syntax-clean; new wiring not yet interaction-tested (sandbox transport down).
- **2026-07-19** — Cropped images fill the frame too (`429b0d4`). A captionless crop rect cover-fills the baseline-locked card (region centered, never a gap); exact crop when the image defines row height, fills by cropping when stretched. `fillCropImg` + `ResizeObserver`. Region-cover-fit math headless-verified.
- **2026-07-19** — Image default = fit-to-height + Undo (`77f46c6`). (1) Uncropped, caption-less images **fill the baseline-locked card frame** (card → flex column, `object-fit:cover`) — no white gap next to tall text. Captioned images stay **fit-to-width**. (2) **Undo/redo** in the editor — Ctrl+Z / Ctrl+Shift+Z (⌘ too), snapshots on every edit (Form + Canvas), restores Form and re-syncs Canvas; canvas iframe forwards the shortcut. Fill-layout + undo-stack headless-verified (undo 9/9); both files syntax-clean.
- **2026-07-19** — Killed the native image-swap `prompt()` (`b243f16`). Clicking an image in Canvas used to fire the ugly browser prompt; replaced with a styled in-page **Replace image** panel — URL field prefilled with the current src, Upload-file button (same postMessage upload path), Set, Cancel/Esc. Component headless-verified 7/7; project.html syntax-clean.
- **2026-07-19** — Crop tool rewrite → real crop **box** (`aa6580c`). Replaced the aspect+focal-dot+zoom model with a draggable/resizable crop rectangle: honest full-image preview, shade dimming outside, drag the box to move, 4 corner handles to resize, Free + aspect-lock presets (continuous lock), and Reset. New crop model `{x,y,w,h}` (normalized); renderer (`imgEl` → `cropfig-rect`) shows any sub-rectangle, computing the box aspect from the image's natural dims on load. Shared component (`cropBoxUI`) in Form **and** Canvas. Legacy `{aspect,fx,fy,zoom}` crops still render. Component headless-verified in an isolated harness — 7/7 (default, free resize, move, render math figAR/width/offset, 1:1 lock → pixel-square, locked resize stays square, reset); both files syntax-clean. Live click-through in the real editor not yet run (file-copy transport was down) — confirm.
- **2026-07-19** — Canvas crop button (`5b53ef9`). Crop was only in the Form editor, so clicking an image in Canvas only offered the URL-swap prompt — undiscoverable. Now each image block's Canvas hover-toolbar has a **Crop** button (◱) that opens an inline crop panel, writing `crop` into the doc via `postDoc()`.
- **2026-07-19** — Row height matching (`7dbf5a5`). Card grid was `align-items:start`, so a short text card beside a tall image left mismatched bottoms (THE GAME vs IMG-01). Now `align-items:stretch`: every card in a row equals the row's max height. Verified headless (short-text + tall-image cards both 573px, Δ0); content stays top-aligned within the matched card.
- **2026-07-19** — Full-width text fills in two columns (`4d1a244`). Classification/lead text blocks are forced full-width but their body was capped at `max-width:64ch`, leaving a narrow left strip + empty right. Now full-width text bodies (`.card.w-full`/`.card.wide`) flow into **two columns** and fill the card; the 64ch cap is scoped to the hero-beside (`.classlead`) layout only. Verified headless (full-width → 2-col fills 818/862px; half-width stays single).
- **2026-07-19** — Dropped the auto `VIMEO`/`MP4`/`VIDEO` source tag from public video cards (`0d2df00`). Source type stays a backend fact (derived from `embed` vs `videoSrc`, still drives the player) but is no longer printed on the page. Card tags in general are auto-derived + not editable — noted as a possible future "optional editable tag" field.
- **2026-07-19** — Image crop tool (`60070a7`). Non-destructive crop — **aspect + draggable focal point + zoom** — on standalone image blocks, media-image blocks, and image+text rows. Editor gets an aspect/zoom picker with a WYSIWYG focal-point drag; the renderer wraps the image in a fixed-aspect box (`object-fit:cover` + `object-position`), so height locks and grid cards baseline-align. Original files untouched. Render verified headless (0 errors); editor parses clean.
- **2026-07-19** — Video + titles (session "Mode Mode project status review"). Editor `media` video blocks now take a native **MP4 upload** with **Loop / Autoplay (muted)** toggles; the renderer emits a native `<video>`, Vimeo still works alongside. Image / media-image cards now show their **Title** instead of the `IMG-NN` auto-label; untitled cards keep auto-numbering (untitled-only count). Renderer verified headless (0 console errors); pushed `b60ca88`. Repo now cloned locally at `~/Documents/GitHub/modemode`.
- **2026-06-28** — Initial snapshot: CMS live, six case-study pages, Supabase backend.

## One line
The full site is live and CMS-driven — a field-map homepage + six case-study pages reading from a **Supabase** backend, edited through a password-gated **studio editor**. Remaining work is real content/photos from Matt, the geometry single-source-of-truth ("split-spine"), media own-hosting, and ship polish.

## Architecture (realized)
- **Two surfaces, one data spine.** `index.html` = the field map (and the field-studio authoring surface for node geometry/colours, now behind `?studio`). `project.html` = one case-study page per project, rendered from that project's JSON document.
- **Data lives in Supabase.** One `public.projects` row per project (`slug` pk · `code` · `name` · `data jsonb`). `project.html`'s single `loadProject()` boundary reads it over PostgREST (anon, RLS public-read) and **falls back to the bundled `content/<slug>.json`** if Supabase is unreachable — the site can't break. The `content/*.json` files remain as the seed source + offline fallback + git history.
- **Media by URL.** Images referenced by URL (currently Matt's Squarespace CDN + a few repo assets); video = **native MP4 upload** (`<video>`, loop/autoplay) **or** Vimeo embed. Media uploads land in the public `project-media` Storage bucket (all MIME types allowed). The org is on **Supabase Pro**, so the per-file cap is raisable in Storage settings — the bucket's current limit still applies to any single upload, so check it before large MP4s.
- **Invariants (frozen):** one self-contained JSON doc per project = one DB row · all reads via `loadProject()` · media by URL · the homepage metaball is mirrored **READ-ONLY** into the project minimap (never refactor `index.html`'s renderer) · BB Strata type + white bg + JetBrains-Mono hairline system + the iOS light-render workaround on all output.

## Live surfaces (all verified headless, 0 console errors)
- **Homepage `index.html`** — field map, present mode by default. 7-project roster (incl. the non-interactive BG layer) baked into `seed()`, overridable via `localStorage mm_field_v2`. Node animation, cursor magnet, click-a-blob → case study, axis hatch-marks running edge-to-edge that **stop at the labels** (knockout). Studio chrome (EDIT/PRESENT + light-dark) is **hidden from the public**; reveal the field studio with `?studio`.
- **Project pages `project.html`** — masonry card-grid: a locked left rail (one fixed logo lockup + identity + a large **animated, textured ambient minimap** that bleeds off-screen left and floats behind the title/content per Julia's mock #2 + flex-fit nav + pager) and content cards. All block types render (text · image · stats · column · imagePara · carousel · cardgrid · media). Full-screen lightbox on every image. prev/next a closed 6-loop. Reads from Supabase.
- **All six projects** live + wired: P-01 EYEKNOW MANOR · P-02 MASSIVE · P-03 GIF · P-04 NIGHTMARE KART · P-05 65 PORTER · P-06 SELECTED ARCADE.
- **CMS editor `editor/index.html`** (supabase-js; `admin.html` redirects here) — email/password login; structured editors for identity, classification (hero/intro/collaborators), and every node (label, blurb, hide, "add content"); all block types with add/remove/move, **each block header labeled by its description + a thumbnail**; image upload to `project-media`; raw-JSON power mode; live Preview ↗; change-password; full-document save. Writes RLS-locked to the editor emails.
- **Backend (Supabase)** — project `modemode` (ref `vjvjparfulrtsxdslrpg`, us-east-1, `https://vjvjparfulrtsxdslrpg.supabase.co`). `projects` table + RLS (public read / editor-email write) + `touch_updated_at` trigger + `project-media` bucket. 6 rows seeded (node counts 15/10/2/5/5/5). Accounts: `comptonjulia@gmail.com`, `mattfryed@gmail.com` (a temporary password was previously committed; rotate any account still using it).

## File map
- `index.html` — homepage field map (+ field studio behind `?studio`).
- `project.html` — case-study renderer (Supabase read + JSON fallback; `loadProject()` is the data boundary).
- `editor/index.html` — the CMS editor; `admin.html` is a redirect to it.
- `content/<slug>.json` — the 6 project docs. **Now the seed/fallback; Supabase is the live source.** Keep them in sync when editing structure by hand (or just edit in the editor, which writes Supabase).
- `assets/` — fonts (BB Strata), the MODE MODE logo SVG, NK stand-in drawings.
- `modemode-withbgnodes.txt` — homepage roster export (the geometry "master," for now).
- `docs/` — this file · `ROADMAP.md` · `ORCHESTRATION.md` · `project-pages_architecture_2026-06-26.md` · `cms-plan.md` · `handoff_*.md` · `master-prompt.md` · `supabase/` (schema.sql + seed + README) · `mockups/mode-mode.pdf`.

## Open / next  (detail in `ROADMAP.md` Phase 9+)
- **Split-spine** (the real architectural item): node geometry/labels/colours live in the homepage roster AND are copied into each project doc's `nodes[].geom`, joined only by `code`, with **no live link** — move a node on the homepage and the saved minimap goes stale. Decide the single source of truth (field-studio writes geometry to Supabase, vs. homepage-master + a sync step). Surface to Julia before building.
- **Real content/photos from Matt:** copy is sample-grade (his site text); Eyeknow event photos are designed placeholders (Kyle's links purged per Julia's rule); the full 22-card tarot deck (6 in repo). See `media-sourcing-policy` (memory): sources = Matt's site / Julia's drive / her Google Drive portfolio; never Kyle.
- **Media own-hosting:** migrate Matt's ~35 Squarespace hotlinks into the `project-media` bucket; repoint `src`.
- **Editor v2:** ✅ **image crop shipped** (`60070a7`, rewritten to a real crop box in `aa6580c`; canvas crop/resize autosave fixed today). ✅ **block drag-drop reorder shipped** (`edcc35d` + follow-ups: ⠿ handle, 4-edge drop detection, stack-underneath). Remaining: the `embed` block (from the memo).
- **Rotate the editor password** — A temporary password was previously committed to this public repository. Rotate any account still using it; removing it from this document does not remove it from Git history.
- **Content hygiene:** delete leftover placeholder blocks in the live docs — eyeknow-manor currently has a blank-title `description` text block (Installation design node) and a `test`/`test` text block (Possessed Ouija Board node) rendering on the live page.
- **Homepage:** cursor becomes the "view project" tab on blob hover (no hand) — the last item from the Jun-28 notes.
- **Auth hardening:** open signup is still enabled (RLS blocks non-editor writes, but accounts can be created) — lock down if desired.
- **Ship:** custom domain (TBD) · SEO/OG meta · optional prerender to static `/<slug>/index.html` for crawlers.

## Decided / settled
Backend = **Supabase** (jsonb document-per-project, not relational). Editor = the on-brand `editor/` tool (Supabase table-UI was the interim). Video = **uploaded MP4 or Vimeo**. Public chrome behind **`?studio`**. Approved media sources only (Matt/local/Drive); **never hotlink Kyle's site**.

## On Julia (account/billing — not code)
- ~~Supabase free tier / idle-pause~~ — **settled. The org (`jalulia's`) is on Supabase Pro, $25/mo** (verified against the Supabase API 2026-07-19). The project is **always-on**; there is no 7-day idle-pause to defend against. Consequence: the **keep-alive GitHub Action** (`.github/workflows/keepalive.yml`) is now **vestigial** — it can be deleted whenever, and costs nothing if left running. Pro also raises the ceilings (8 GB DB, 100 GB storage + egress, PITR available) — all far beyond this site's needs, so capacity is a non-issue.
- Production **domain undecided**; the site serves at `https://jalulia.github.io/modemode/` today (homepage), `/modemode/<slug>` via `project.html?p=`, `/modemode/editor` (CMS).
